apiVersion: v1
kind: Service
metadata:
  name: stellar-tipjar-backend
spec:
  selector:
    app: stellar-tipjar-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
